plugins {
    id 'java'
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.9'
    id 'org.jetbrains.kotlin.jvm' version '1.4.30-M1'
}

group 'javafx.skeleton'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    maven { url 'http://dist.wso2.org/maven2/'}
    maven { url 'https://dl.bintray.com/kotlin/kotlin-eap' }
    flatDir {
        dirs 'lib'
    }
}

test {
    useJUnitPlatform()
}

dependencies {
    testCompile group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.6.2'
    testCompile group: 'io.mockk', name: 'mockk', version: '1.10.0'
    testCompile 'com.willowtreeapps.assertk:assertk-jvm:0.23'
    testCompile "org.junit.jupiter:junit-jupiter-params:5.6.2"
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.6.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
    implementation group: 'com.googlecode.javaewah.wso2', name: 'JavaEWAH', version: '0.3.2.wso2v1'
    implementation group: 'org.slf4j', name: 'slf4j-api', version: '1.7.30'
    compile group: 'org.eclipse.jgit', name: 'org.eclipse.jgit', version: 'inigo-custom'
    compile group: 'org.jetbrains.kotlinx', name: 'kotlinx-coroutines-core', version: '1.4.2'

}

test {
    useJUnitPlatform()
}

javafx {
    version = "15"
    modules = [ 'javafx.web', 'javafx.swing', 'javafx.media', 'javafx.base', 'javafx.graphics',  'javafx.controls', 'javafx.fxml' ]
}

mainClassName = 'xarmanta.launch.Launcher'

//To create a runnable jar  in GradleJavaProject\build\libs then java -jar GradleJavaProject.jar
jar {
    manifest {
        attributes 'Main-Class': 'xarmanta.launch.Launcher'
    }
}

compileJava.options.encoding = 'UTF-8'
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
compileKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
compileTestKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
